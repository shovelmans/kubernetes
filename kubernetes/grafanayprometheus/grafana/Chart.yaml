apiVersion: v2
name: grafana-custom
description: Helm chart wrapper para desplegar Grafana con mis valores
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: grafana
    version: "8.3.*"   # versión actual en grafana repo (ajústala si quieres)
    repository: "https://grafana.github.io/helm-charts"
