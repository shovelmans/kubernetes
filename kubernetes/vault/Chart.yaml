apiVersion: v2
name: vault-wrapper
description: Helm chart wrapper para desplegar Vault con mis valores
type: application
version: 0.1.0

dependencies:
  - name: vault
    version: 0.30.1   # versión estable del chart de Vault (soporta server.ingress)
    repository: https://helm.releases.hashicorp.com
