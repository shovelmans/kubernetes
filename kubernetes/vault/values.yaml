server:
  dev:
    enabled: true
  dataStorage:
    enabled: false

  service:
    type: ClusterIP

  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: vault.jorge.local
        paths:
          - path: /
            pathType: Prefix

ui:
  enabled: true
