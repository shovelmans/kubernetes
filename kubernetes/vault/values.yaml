server:
  dev:
    enabled: true

ui:
  enabled: true

  service:
    type: ClusterIP

  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: vault.jorge.local
        paths:
          - path: /
            pathType: Prefix
