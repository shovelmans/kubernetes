server:
  dev:
    enabled: true

ui:
  enabled: true
  serviceType: ClusterIP
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: vault.jorge.local
        paths:
          - path: /
            pathType: Prefix
