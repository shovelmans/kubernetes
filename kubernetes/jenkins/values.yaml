controller:
  adminUser: admin
  existingSecret: jenkins-admin-secret
  existingSecretKey: jenkins-admin-password

  persistence:
    enabled: true
    size: 20Gi
    storageClass: "rook-ceph-block"

  service:
    type: NodePort
    nodePort: 32000

  jenkinsUrl: "http://206.189.179.134:32000"
