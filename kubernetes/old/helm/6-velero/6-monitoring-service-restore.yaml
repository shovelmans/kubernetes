apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring-restore
spec:
  selector:
    app: grafana
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 32001   # elige otro puerto distinto de 32000
