apiVersion: v2
name: ingress
description: Ingress Controller NGINX gestionado por ArgoCD
type: application
version: 1.0.0

dependencies:
  - name: ingress-nginx
    version: "4.11.2"   # última versión estable a día de hoy
    repository: "https://kubernetes.github.io/ingress-nginx"
